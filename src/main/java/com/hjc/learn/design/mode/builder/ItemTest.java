package com.hjc.learn.design.mode.builder;

/**
 * 普通方式创建商品测试
 *
 * @author houjichao
 */
public class ItemTest {

    public static void main(String[] args) {
        /*Item item = new Item();
        if (client 创建普通商品) {
            item.setItemName("");
            item.setType(1);
        }

        if (client 创建电子卡券类) {
            item.setItemName("");
            item.setType(1);
            item.setCode("");
        }

        if (client 创建视频类) {
            item.setItemName("");
            item.setType(1);
            item.setUrl("");
        }
        return item ;*/
    }
}
